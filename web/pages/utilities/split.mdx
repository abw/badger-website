import SplitCode  from './_examples/Split.jsx'
import SplitSrc   from './_examples/Split.jsx?raw'
import Example    from '@/code/Example.jsx'
import PropsTable from '@/src/ui/PropsTable.jsx'

# Split

The `Split` component renders two blocks of content side-by-side when
the containing element is sufficiently wide, or stacked on top of each other
if not.

<Example
  Component={SplitCode}
  code={SplitSrc}
  expand
/>

## Properties

<PropsTable
  items={[
    ['split',     '2',      'The number of split sections'],
    ['gap',       '8',      'The gap between sections in gap units (0.25rem)'],
    ['hgap',      'gap',    'The horizontal gap between sections when displayed next to each other. Defaults to the value for `gap`.'],
    ['vgap',      'gap',    'The vertical gap between sections when displayed next to each other. Defaults to the value for `gap`.'],
    ['stack',     'laptop', 'The breakpoint at which the sections stack, defaults to `laptop` (960px).'],
    ['align',     'top',    'The vertical alignment for the sections.'],
    ['className', '',       'An additional CSS class for the container.'],
  ]}
/>
