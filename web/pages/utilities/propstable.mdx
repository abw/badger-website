import PTCode     from './_examples/PropsTable.jsx'
import PTSrc      from './_examples/PropsTable.jsx?raw'
import Example    from '@/code/Example.jsx'
import PropsTable from '@/src/ui/PropsTable.jsx'
import { Heading } from '@/page/Heading.jsx'

# PropsTable

The `PropsTable` component can be used to generated a table showing properties
for a component.

<Example
  Component={PTCode}
  code={PTSrc}
  expand
/>

<Heading title="Properties"/>

<PropsTable
  items={[
    [
      'items', '',
      [
        'An array of items, each containing 3 elements: the property `name`, `default` value and `description`.',
        'The `description` can be either a string or an array of strings which will be rendered as separate paragraphs.',
        'The `name` and `default` will be rendered in monospaced font.  The description can contain backticks around an extract to render in monospaced font.'
      ]
    ],
  ]}
/>
