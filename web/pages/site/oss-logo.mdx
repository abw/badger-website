import OSSCode  from './_examples/OSS.jsx'
import OSSSrc   from './_examples/OSS.jsx?raw'
import Example  from '@/code/Example.jsx'

# OSSLogo

The `OSSLogo` component renders the "Open Source Software / Opinionated
Selfish Software" SVG logo.  You can add a CSS class using the `className`
property.  It also has the `oss-logo` CSS class added to it if you want to
target it in CSS.

<Example
  Component={OSSCode}
  code={OSSSrc}
  expand
/>
