import HeroCode from './_examples/UseHero.jsx'
import HeroSrc  from './_examples/UseHero.jsx?raw'
import Example  from '@/code/Example.jsx'

# Hero

The `Hero` component is for rendering a hero block, typically at the start of
the home page.

<Example
  Component={HeroCode}
  code={HeroSrc}
  expand
/>
