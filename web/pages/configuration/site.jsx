import React from 'react'

const Site = () =>
  <div>
    <h1>Site configuration</h1>
    <p>
      The <code>site</code> option is passed to the <code>Provider</code> component
      to define site configuration options.
    </p>
    <p>
      TODO: site configuration options
    </p>
  </div>

export default Site