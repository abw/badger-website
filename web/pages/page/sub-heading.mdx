import Example  from '@/code/Example.jsx'
import SubComp from './_examples/subheading.jsx'
import SubCode from './_examples/subheading.jsx?raw'
import MdxComp from './_examples/subheading.mdx'
import MdxCode from './_examples/subheading.mdx?raw'
import { PageTocLink } from '@/web/site/Links.jsx'

# SubHeading

The `SubHeading` component can be used to render a heading block as an `h3`
element.

It accepts a `text` property for the title text (or `title` as an alias for
`text`), or a `code` property for text that should be displayed in a
monospaced font.

It also inserts a heading entry in the table of contents which will be
displayed in the sidebar and any <PageTocLink/> included in the page.

<Example
  Component={SubComp}
  code={SubCode}
  highlightLines="2,7,11"
  expand
/>

When using MDX this element is rendered for third level headings indicated
by the `###` prefix on a line.

<Example
  Component={MdxComp}
  code={MdxCode}
  highlightLines="3,7"
  expand
/>
