import Example  from '@/code/Example.jsx'
import SectComp from './_examples/section.jsx'
import SectCode from './_examples/section.jsx?raw'
import { HeadingLink } from '@/web/site/Links.jsx'

# Section

The `Section` component can be used to render a page section.

It renders a `section` HTML element containing a <HeadingLink/> component
and any children.

<Example
  Component={SectComp}
  code={SectCode}
  highlightLines="2,7,11"
  expand
/>
