import Example  from '@/code/Example.jsx'
import SectComp from './_examples/subsection.jsx'
import SectCode from './_examples/subsection.jsx?raw'
import { SubHeadingLink } from '@/web/site/Links.jsx'

# SubSection

The `SubSection` component can be used to render a page section.

It renders a `section` HTML element containing a <SubHeadingLink/> component
and any children.

<Example
  Component={SectComp}
  code={SectCode}
  highlightLines="2,7,11"
  expand
/>
