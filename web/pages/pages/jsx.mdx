import Example from '@/code/Example.jsx'
import JsxComp from './_examples/jsxfile.jsx'
import JsxCode from './_examples/jsxfile.jsx?raw'
import Link    from '@/ui/Link.jsx'

# JSX Pages

JSX pages (with a `.jsx` file extension) can contain any React markup.  There
should be a single default export which should be the component that will be
rendered to generate the page content.

<Example
  Component={JsxComp}
  code={JsxCode}
/>