import Example from '@/code/Example.jsx'
import JsxComp from '@/snippets/pages/jsxfile.jsx'
import JsxCode from '@/snippets/pages/jsxfile.jsx?raw'
import Link    from '@/ui/Link.jsx'

# JSX Pages

JSX pages can contain any React markup.  There should be a single default
export which should be the component that will be rendered to generate the
page content.

<Example
  Component={JsxComp}
  code={JsxCode}
/>