import { now } from '@abw/badger-timestamp'

export const smallScreenBreakpoints = {
  mobile: true,
  tablet: true
}
export const defaultSite = {
  version:  '0.0.1',
  title:    'Badger Website',
  date:     now().date()
}
